node_modules
out
dist
dist-electron
*.log
Backup/
*.zip
*.exe
# Scratch folder of generated icon-design candidates that keeps getting swept
# into commits by accident. The icon actually shipped is build/icon.ico.
/icons/

# Electron runtime payload, if an installer ever writes into this folder again.
# It happened on 2026-08-18: a silent install with no /D fell back to the
# launching process's working directory and unpacked the whole app in here.
# These are build output wherever they appear, never source.
/locales/
/resources/
/Uninstall Game Browser.exe
*.pak
*.dll
*.bin
icudtl.dat
LICENSE.electron.txt
LICENSES.chromium.html
vk_swiftshader_icd.json
